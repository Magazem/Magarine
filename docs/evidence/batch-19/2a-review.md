# Mini-phase 2A review against ruling 37 (read-only). Suite: 892 tests, 890 pass, 1 fail (below).

[Critical] packages/core/src/adapters/claudeCli.ts:603 - pushes the literal `'You are a worker.'` after `--append-system-prompt` instead of `renderProfileSystemPrompt(input.ticket.profile)`; the exported renderer is never called, so no worker is ever told who it is - claudeCliProfileArgv.test.ts second test FAILS (actual `'You are a worker.'`, expected `You are Developer, implementation with "quotes", ...`); acceptance 4 and 6 not met.

[High] packages/core/src/proposal.ts:641 - `profile`/`model` exclusivity is only checked within one command, never against the ticket's EXISTING row; store.ts updateTicketFields then throws a plain Error inside managerApply's transaction (no catch there or at scheduler.ts:591), so the proposal is not rejected as malformed but escapes the apply - reproduced: ticket created with `model: 'claude-sonnet-5'`, then `update_ticket { ticket_id, profile: 'Developer', profile_reason: 'x' }` -> THREW "choose a profile or a model, not both"; same for `update_ticket { model }` on a profile ticket. ProposalBoard.tickets carries no model/profileId, so the validator cannot know. Side effect: a bare-model ticket can never be moved onto a profile by proposal (no way to clear `model`).

[Medium] packages/core/src/commands/board.ts:342 - formatBoard renders `model X (modelReason)` but never `profile` or `profileReason`; the text board row for a ticket with profile Developer and a reason shows neither, so "shown the same way modelReason is shown today" holds only for the JSON BoardTicket, not the rendered board - input: `magarine board` on the acceptance-2 ticket.

[Low] packages/core/src/proposal.ts:355 - `profile_reason` without `profile` is accepted and persisted on a profile-less ticket - reproduced: `update_ticket { ticket_id, profile_reason: 'orphan reason' }` -> applied, ticket.profileReason = 'orphan reason', board shows a reason with no profile.
[Low] packages/core/src/proposal.ts:394 - validator also accepts a profile ID (`activeProfileIds.has`) though grammar, ruling and roster say name; and it lower-cases with toLocaleLowerCase while store.ts:1302 matches with SQLite COLLATE NOCASE (ASCII only) - a profile named `Éditeur` referenced as `éditeur` passes validation, then resolveWorkerProfileRef throws NoSuchWorkerProfileError out of the transaction.
[Low] packages/core/src/scheduler.test.ts:622 and managerEnvelope.test.ts:362 - "manager envelope carries no profile" tests cannot fail: no surface (CLI, route, proposal) can give a manager-kind ticket a profile, so the assertion is vacuous and the createRun guard at store.ts:934 is never exercised end to end. Verify/Manager runs are only checked at envelope level, never at real-adapter argv level as acceptance 4 words it.
[Low] packages/core/src/managerEnvelope.test.ts:297 - the roster "exactly" test is substring `includes` on six names; the "no more" half is proven only for the one retired name, and line 335's `/x\b/` price-ratio check matches any word ending in x (e.g. "prefix").
