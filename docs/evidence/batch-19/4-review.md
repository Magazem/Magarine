# Batch 19 mini-phase 4, daemon half: review against ruling 40 (Reviewer 4, 2026-09-23)
Method: read the diff; ran the 5 touched test files in a temp copy (181/191; the 10 failures are claudeCli fixture-path noise, 46/46 pass in the real tree); probed a real FakeAdapter loop with an on-disk DB through success, late-live, cancel and shutdown, then grepped the .db, WAL and every state-dir file: 0 occurrences of the command text. No leak on disk, in events, activity or stream was found.
Critical: none
[High] packages/core/src/scheduler.ts:1422 - the observeLive listener is never unsubscribed and never checks that the run is still live, so a live signal arriving after settle re-creates the map entry and nothing ever clears it (daemon.stop sweeps `live`, not `liveRuns`; the observe path's late-event guard has no live-channel twin) - FakeAdapter setLiveToolUse(delayMs 300) + setScript succeed(delayMs 0): ticket DONE, loop.liveRuns still holds the command after cancelTicket/stop, GET /runs/{id}/live is 200 for a settled run (acceptance 2) and the command lives in memory for the daemon's lifetime; real adapter hits the same window on cancel/timeout/budget stop since killTree resolves before buffered stdout drains (README "keeps talking after being stopped").
[Medium] packages/core/src/verifier.ts:357 (deps built at scheduler.ts:132 without liveRuns) - verifier runs never register observeLive, so a verifier running the test suite through Bash always 404s on /runs/{id}/live - REVIEW ticket, verifier in flight running `pnpm test`: route 404, the owner's "is it stuck" case is unanswerable for the run most likely to be stuck.
[Medium] packages/core/src/daemonApi.ts:605 - 404 for a RUNNING run with no tool use yet contradicts ruling 40 section 2 ("for a RUNNING run ... or 404 once the run has settled") and hides lastProgressAt exactly when a worker hangs before its first tool call - a run stuck at startup (auth prompt, hung CLI) returns 404 indistinguishable from "settled", so the page cannot show the stuck measurement or know whether to keep polling; should be 200 with tool null and lastProgressAt.
[Medium] packages/core/src/daemonApi.test.ts:1649 - acceptance 3's run uses script kind 'progress', which never terminates, so no terminal payload (worker_done, worker_failure, run row on finishRun, cancel event) is ever scanned - a mutation that copies `detail` into a terminal event or the run row passes this test; the only mutation it catches is one on the worker_progress path.
[Low] packages/core/src/scheduler.test.ts:2042 - "untouched when the caller supplies none" asserts only started.length === 1; it passes unchanged if observeLive is called - remove the `if (deps.liveRuns)` guard: test still green.
[Low] packages/core/src/daemon.test.ts:851 - orphan-recovery test is tautological: a fresh Map is empty by construction and recovery has no liveRuns code path - it cannot fail for any change to recovery.ts.
[Low] packages/core/src/daemonApi.test.ts:1575 - "401 with no token" sends `Bearer wrong-token`; isAuthorized's missing-header branch is not exercised for this route - request with no Authorization header untested.
[Low] packages/core/src/adapters/claudeCli.ts:475 - only the first content block of a line is inspected; a line whose content is [text, tool_use Bash] returns null - moot today (fixture 2026-09-12T14-15-13-624Z emits one block per line) but brittle if the CLI batches blocks.
Judgements asked for: `since` resetting per tool use is honest (ruling: "when it started" = the command; the real stream emits each tool_use once, so no spurious reset). `detail: ''` for non-Bash is honest against the ruling's definition; the page half must render it as "no command", which is the element-field table's job. describeProgress/"testing" predicate: untouched in the diff, its existing tests pass. Every settle path clears the map (success, failure, cancel, timeout, shutdown, budget stop, adapter_unavailable) EXCEPT the post-settle re-insertion above.
