# Batch 20B review: New project form, checked against ruling 42 (Reviewer 20B, 2026-09-23)
Critical: none. BACKSLASHES ARE KEPT (checked in real headless Chrome with Input.insertText): I typed `C:\Users\yazan\AppData\Local\Temp\magarine-test-rev20b2-It07Yq\no such folder` and the refusal echoes that exact path; a real temp folder was created and the scope note names `...\real-dEdYI3\SCOPE.md`. The `C:osucholder` in the earlier shots was my own script's unescaped JS string (`\n`, `\f`), not the page. 20b-c-refusal.png has been retaken correctly. 20b-d-900-open.png still shows that script artefact in the Directory field and the refusal; its layout is valid.
[High] packages/core/ui/app.js:1693 - after a 201 the page selects the new project but never refills an open Settings panel (the select-change handler at :2352 does refill it), so the panel keeps showing the old project's models and cap, and "Save project settings" writes them onto the new project - with Settings open on "Scripted" (cap 2), creating "New" with no cap then pressing Save leaves New with maxParallelWorkers 2 instead of null (domHarness probe on a temp copy).
[High] packages/core/ui/skin-brutalist.css:76 - in the default board view (body height 100vh) the form takes the gate's grid row and squeezes the board, roster, Needs you and Activity into what is left, so it covers the owner's work - at 1440x900 20b-b-open.png leaves the board 289px of 838 and 20b-c-refusal.png even less; with "Models and worker cap" open, 20b-b2-open-models.png leaves the board 40px and Needs you 32px, which breaks ruling 15's "Needs you on screen in every view".
[Medium] packages/core/ui/skin-brutalist.css:771 - the form looks bolted on: a 562px column at the left of a full-width band, with about 880px of empty black to its right and no surface or edge tying it to the regions below - 20b-b-open.png, 20b-c-refusal.png. At 900px (20b-d-900-open.png) the page scrolls normally, and nothing overlaps or is clipped.
[Low] packages/core/ui/app.js:1694 - a successful create calls closeScopeEditor, which silently drops an unsaved scope draft in the previously selected project (reopening the editor resets it to the file); this matches the select-change precedent, but a create is not an explicit switch - open Edit on the scope, type, create a project: the draft is gone.
[Low] packages/core/ui/app.js:1681 - `isFinite(Number(cap))` reinterprets typed text instead of letting the daemon refuse it - a Worker cap of `0x2` creates a project with cap 2 and no refusal. Same rule as saveProject.
Checked, no defect: refusals are verbatim and clear nothing (4 daemon sentences); typing, the open disclosure and a chosen model survive two polls; a double click makes one project; the form is global, so a project switch while it is open changes nothing in it; the table's POST /projects row matches daemonApi.ts:397 (201, row + scopeNote; 400 `{error}`); page.test.ts:149 scans all of app.js/index.html for `new Notification`/`beforeunload`, and the new code has neither.
Tests: mutations on a temp copy (drop the gate hiding, the npBusy guard, both double-submit guards, the selection, the re-enable) each failed a named newProject test. No test covers the Settings leak or the layout. The new tests pass 8/8 and page.test.ts passes 16/16 in the repo.
