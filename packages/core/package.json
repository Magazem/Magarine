{
  "name": "@magarine/core",
  "version": "0.1.0",
  "private": true,
  "description": "Magarine orchestrator daemon core: state machine, scheduler, adapters, CLI.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test",
    "cli": "node src/cli.ts"
  },
  "devDependencies": {}
}
